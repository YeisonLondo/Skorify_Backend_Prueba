name: SCA - Trivy

on:
  pull_request:
    branches:
      - main
      - develop
      - release/*
    types:
      - opened
      - synchronize
      - reopened

jobs:
  sca:
    uses: YeisonLondo/Skorify_DevOps_Prueba/.github/workflows/sca-generic.yml@main